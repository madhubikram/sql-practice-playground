export const moviesQuestions = [
{id:'mv1',db:'movies',difficulty:'beginner',title:'All Movies',roleplay:'The archive needs a complete film catalog.',task:'List all movie titles.',answer:'SELECT title FROM movies;',concepts:['SELECT'],altAnswers:[]},
{id:'mv2',db:'movies',difficulty:'beginner',title:'Recent Films',roleplay:'A reviewer wants films from 2020 onwards.',task:'Find movies released in 2020 or later.',answer:'SELECT title, release_year FROM movies WHERE release_year >= 2020;',concepts:['WHERE','>='],altAnswers:[]},
{id:'mv3',db:'movies',difficulty:'beginner',title:'Top Rated',roleplay:'Streaming service wants the best-rated films.',task:'Find movies with rating above 8.5.',answer:'SELECT title, rating FROM movies WHERE rating > 8.5 ORDER BY rating DESC;',concepts:['WHERE','ORDER BY'],altAnswers:[]},
{id:'mv4',db:'movies',difficulty:'beginner',title:'Sci-Fi Movies',roleplay:'A sci-fi channel is curating content.',task:'List all movies in the Sci-Fi genre.',answer:"SELECT title FROM movies WHERE genre = 'Sci-Fi';",concepts:['WHERE'],altAnswers:[]},
{id:'mv5',db:'movies',difficulty:'beginner',title:'Billion Dollar Films',roleplay:'Studio wants their biggest earners.',task:'Find movies with revenue over 1 billion.',answer:'SELECT title, revenue FROM movies WHERE revenue > 1000000000;',concepts:['WHERE','>'],altAnswers:[]},
{id:'mv6',db:'movies',difficulty:'beginner',title:'Longest Movies',roleplay:'Cinema chain needs scheduling info.',task:'List the 5 longest movies by duration.',answer:'SELECT title, duration FROM movies ORDER BY duration DESC LIMIT 5;',concepts:['ORDER BY','LIMIT'],altAnswers:[]},
{id:'mv7',db:'movies',difficulty:'beginner',title:'Oscar Winners',roleplay:'Awards show needs actor data.',task:'Find actors with at least 1 Oscar win.',answer:'SELECT name, oscar_wins FROM actors WHERE oscar_wins >= 1;',concepts:['WHERE','>='],altAnswers:[]},
{id:'mv8',db:'movies',difficulty:'beginner',title:'Count by Genre',roleplay:'Analytics wants genre distribution.',task:'Count movies in each genre.',answer:'SELECT genre, COUNT(*) AS count FROM movies GROUP BY genre;',concepts:['GROUP BY','COUNT'],altAnswers:[]},
{id:'mv9',db:'movies',difficulty:'beginner',title:'Movie Budget Stats',roleplay:'Financial team needs budget overview.',task:'Find average, min, and max movie budget.',answer:'SELECT AVG(budget) AS avg_budget, MIN(budget) AS min_budget, MAX(budget) AS max_budget FROM movies;',concepts:['AVG','MIN','MAX'],altAnswers:[]},
{id:'mv10',db:'movies',difficulty:'beginner',title:'Director Search',roleplay:'Find a specific director.',task:"Find directors whose name contains 'Nolan'.",answer:"SELECT * FROM directors WHERE name LIKE '%Nolan%';",concepts:['LIKE'],altAnswers:[]},
{id:'mv11',db:'movies',difficulty:'beginner',title:'British Actors',roleplay:'A UK film festival wants British talent.',task:'Find all British actors.',answer:"SELECT name FROM actors WHERE nationality = 'British';",concepts:['WHERE'],altAnswers:[]},
{id:'mv12',db:'movies',difficulty:'beginner',title:'Profitable Movies',roleplay:'Find films that made a profit.',task:'List movies where revenue exceeds budget.',answer:'SELECT title, budget, revenue, (revenue - budget) AS profit FROM movies WHERE revenue > budget;',concepts:['arithmetic','WHERE'],altAnswers:[]},
{id:'mv13',db:'movies',difficulty:'beginner',title:'Total Revenue',roleplay:'Studio needs total earnings.',task:'Calculate total revenue across all movies.',answer:'SELECT SUM(revenue) AS total_revenue FROM movies;',concepts:['SUM'],altAnswers:[]},
{id:'mv14',db:'movies',difficulty:'beginner',title:'Distinct Genres',roleplay:'Content team needs genre list.',task:'Find all unique genres in the movies table.',answer:'SELECT DISTINCT genre FROM movies;',concepts:['DISTINCT'],altAnswers:[]},
{id:'mv15',db:'movies',difficulty:'beginner',title:'Reviews Count',roleplay:'How many reviews are in the system?',task:'Count total reviews.',answer:'SELECT COUNT(*) AS total_reviews FROM reviews;',concepts:['COUNT'],altAnswers:[]},
{id:'mv16',db:'movies',difficulty:'beginner',title:'Average Rating',roleplay:'What is the overall movie quality?',task:'Find the average movie rating.',answer:'SELECT ROUND(AVG(rating), 2) AS avg_rating FROM movies;',concepts:['AVG','ROUND'],altAnswers:['SELECT AVG(rating) AS avg_rating FROM movies;']},
{id:'mv17',db:'movies',difficulty:'beginner',title:'Movies in 90s',roleplay:'Nostalgia channel wants 90s content.',task:'Find movies released between 1990 and 1999.',answer:'SELECT title, release_year FROM movies WHERE release_year BETWEEN 1990 AND 1999;',concepts:['BETWEEN'],altAnswers:[]},
{id:'mv18',db:'movies',difficulty:'beginner',title:'Low Budget Gems',roleplay:'Indie film festival curates low-budget hits.',task:'Find movies with budget under 30 million that earned over 200 million.',answer:'SELECT title, budget, revenue FROM movies WHERE budget < 30000000 AND revenue > 200000000;',concepts:['AND','WHERE'],altAnswers:[]},
{id:'mv19',db:'movies',difficulty:'intermediate',title:'Movie Directors',roleplay:'Credits page needs movie-director pairs.',task:'List each movie title with its director name.',answer:'SELECT m.title, d.name AS director FROM movies m JOIN directors d ON m.director_id = d.id;',concepts:['JOIN'],altAnswers:[]},
{id:'mv20',db:'movies',difficulty:'intermediate',title:'Actor Filmography',roleplay:'Build actor profile pages.',task:'List all actors and the movies they appeared in.',answer:'SELECT a.name AS actor, m.title AS movie FROM cast_members c JOIN actors a ON c.actor_id = a.id JOIN movies m ON c.movie_id = m.id;',concepts:['multiple JOINs'],altAnswers:[]},
{id:'mv21',db:'movies',difficulty:'intermediate',title:'Director Avg Rating',roleplay:'Rank directors by quality.',task:'Show average rating per director.',answer:'SELECT d.name, ROUND(AVG(m.rating), 2) AS avg_rating FROM movies m JOIN directors d ON m.director_id = d.id GROUP BY d.name ORDER BY avg_rating DESC;',concepts:['JOIN','GROUP BY','AVG'],altAnswers:[]},
{id:'mv22',db:'movies',difficulty:'intermediate',title:'Most Reviewed',roleplay:'Which movie has the most buzz?',task:'Find the movie with the most reviews.',answer:'SELECT m.title, COUNT(r.id) AS review_count FROM movies m JOIN reviews r ON m.id = r.movie_id GROUP BY m.title ORDER BY review_count DESC LIMIT 1;',concepts:['JOIN','GROUP BY','COUNT'],altAnswers:[]},
{id:'mv23',db:'movies',difficulty:'intermediate',title:'Actor Movie Count',roleplay:'Who is the busiest actor?',task:'Count how many movies each actor has appeared in.',answer:'SELECT a.name, COUNT(c.movie_id) AS movie_count FROM actors a JOIN cast_members c ON a.id = c.actor_id GROUP BY a.name ORDER BY movie_count DESC;',concepts:['JOIN','GROUP BY','COUNT'],altAnswers:[]},
{id:'mv24',db:'movies',difficulty:'intermediate',title:'Revenue by Director',roleplay:'Which directors make the most money?',task:'Show total revenue per director.',answer:'SELECT d.name, SUM(m.revenue) AS total_rev FROM movies m JOIN directors d ON m.director_id = d.id GROUP BY d.name ORDER BY total_rev DESC;',concepts:['JOIN','GROUP BY','SUM'],altAnswers:[]},
{id:'mv25',db:'movies',difficulty:'intermediate',title:'CASE Rating Label',roleplay:'Categorize movies by rating quality.',task:"Use CASE to label movies as 'Masterpiece' (>=9), 'Great' (>=8), 'Good' (>=7), or 'Average'.",answer:"SELECT title, rating, CASE WHEN rating >= 9 THEN 'Masterpiece' WHEN rating >= 8 THEN 'Great' WHEN rating >= 7 THEN 'Good' ELSE 'Average' END AS label FROM movies;",concepts:['CASE WHEN'],altAnswers:[]},
{id:'mv26',db:'movies',difficulty:'intermediate',title:'Review Scores vs Rating',roleplay:'Compare critic and audience scores.',task:'Show movie title, its rating, and average review score.',answer:'SELECT m.title, m.rating, ROUND(AVG(r.score), 2) AS avg_review FROM movies m JOIN reviews r ON m.id = r.movie_id GROUP BY m.title, m.rating;',concepts:['JOIN','GROUP BY','AVG'],altAnswers:[]},
{id:'mv27',db:'movies',difficulty:'intermediate',title:'Directors Multi-Film',roleplay:'Find prolific directors.',task:'Find directors who directed more than 2 movies.',answer:'SELECT d.name, COUNT(m.id) AS films FROM directors d JOIN movies m ON d.id = m.director_id GROUP BY d.name HAVING COUNT(m.id) > 2;',concepts:['HAVING','COUNT'],altAnswers:[]},
{id:'mv28',db:'movies',difficulty:'intermediate',title:'Profit Margin',roleplay:'Calculate ROI for each film.',task:'Show title, budget, revenue, and profit margin percentage.',answer:'SELECT title, budget, revenue, ROUND((revenue - budget) * 100.0 / budget, 1) AS margin_pct FROM movies WHERE budget > 0 ORDER BY margin_pct DESC;',concepts:['arithmetic'],altAnswers:[]},
{id:'mv29',db:'movies',difficulty:'intermediate',title:'UNION Talent',roleplay:'Combine actor and director names.',task:'Create a combined list of all actor and director names.',answer:"SELECT name, 'Actor' AS role FROM actors UNION SELECT name, 'Director' AS role FROM directors;",concepts:['UNION'],altAnswers:[]},
{id:'mv30',db:'movies',difficulty:'intermediate',title:'LEFT JOIN Reviews',roleplay:'Find movies without reviews.',task:'Show all movies and their review count, including those with 0 reviews.',answer:'SELECT m.title, COUNT(r.id) AS reviews FROM movies m LEFT JOIN reviews r ON m.id = r.movie_id GROUP BY m.title ORDER BY reviews;',concepts:['LEFT JOIN','COUNT'],altAnswers:[]},
{id:'mv31',db:'movies',difficulty:'intermediate',title:'Genre Revenue',roleplay:'Which genre is most profitable?',task:'Show total revenue by genre.',answer:'SELECT genre, SUM(revenue) AS total_rev FROM movies GROUP BY genre ORDER BY total_rev DESC;',concepts:['GROUP BY','SUM'],altAnswers:[]},
{id:'mv32',db:'movies',difficulty:'intermediate',title:'Lead Actors',roleplay:'Find lead actors (billing order 1).',task:'List actors who had billing_order = 1 and their movies.',answer:'SELECT a.name, m.title FROM cast_members c JOIN actors a ON c.actor_id = a.id JOIN movies m ON c.movie_id = m.id WHERE c.billing_order = 1;',concepts:['JOIN','WHERE'],altAnswers:[]},
{id:'mv33',db:'movies',difficulty:'intermediate',title:'Decade Analysis',roleplay:'Analyze movies by decade.',task:"Show decade (e.g., 1990, 2000, 2010) and count of movies.",answer:"SELECT (release_year / 10) * 10 AS decade, COUNT(*) AS count FROM movies GROUP BY decade ORDER BY decade;",concepts:['arithmetic','GROUP BY'],altAnswers:[]},
{id:'mv34',db:'movies',difficulty:'intermediate',title:'Best Reviewed Movie',roleplay:'Find the movie with the highest avg review score.',task:'Find the movie with the highest average review score.',answer:'SELECT m.title, ROUND(AVG(r.score), 2) AS avg_score FROM movies m JOIN reviews r ON m.id = r.movie_id GROUP BY m.title ORDER BY avg_score DESC LIMIT 1;',concepts:['JOIN','GROUP BY','AVG'],altAnswers:[]},
{id:'mv35',db:'movies',difficulty:'intermediate',title:'Canadian Directors',roleplay:'Film festival features Canadian talent.',task:'List movies directed by Canadian directors.',answer:"SELECT m.title, d.name FROM movies m JOIN directors d ON m.director_id = d.id WHERE d.nationality = 'Canadian';",concepts:['JOIN','WHERE'],altAnswers:[]},
{id:'mv36',db:'movies',difficulty:'intermediate',title:'Director Oscar Count',roleplay:'Awards analysis.',task:'Show each director and their total Oscar wins.',answer:'SELECT name, oscar_wins FROM directors ORDER BY oscar_wins DESC;',concepts:['ORDER BY'],altAnswers:[]},
{id:'mv37',db:'movies',difficulty:'advanced',title:'Revenue Rank',roleplay:'Rank movies by box office.',task:'Use RANK() to rank movies by revenue.',answer:'SELECT title, revenue, RANK() OVER (ORDER BY revenue DESC) AS rev_rank FROM movies;',concepts:['RANK','window function'],altAnswers:[]},
{id:'mv38',db:'movies',difficulty:'advanced',title:'Genre Rank',roleplay:'Rank movies within each genre.',task:'Rank movies by rating within each genre.',answer:'SELECT title, genre, rating, RANK() OVER (PARTITION BY genre ORDER BY rating DESC) AS genre_rank FROM movies;',concepts:['RANK','PARTITION BY'],altAnswers:[]},
{id:'mv39',db:'movies',difficulty:'advanced',title:'Running Revenue',roleplay:'Cumulative box office over time.',task:'Show running total of revenue ordered by release year.',answer:'SELECT title, release_year, revenue, SUM(revenue) OVER (ORDER BY release_year) AS cumulative_rev FROM movies;',concepts:['SUM OVER','window function'],altAnswers:[]},
{id:'mv40',db:'movies',difficulty:'advanced',title:'Above Average Budget',roleplay:'Find expensive movies using subquery.',task:'Find movies with budget above average.',answer:'SELECT title, budget FROM movies WHERE budget > (SELECT AVG(budget) FROM movies);',concepts:['subquery'],altAnswers:[]},
{id:'mv41',db:'movies',difficulty:'advanced',title:'Best Movie Per Director',roleplay:'Each directors magnum opus.',task:'Find the highest-rated movie for each director.',answer:'SELECT title, director_id, rating FROM (SELECT title, director_id, rating, ROW_NUMBER() OVER (PARTITION BY director_id ORDER BY rating DESC) AS rn FROM movies) sub WHERE rn = 1;',concepts:['ROW_NUMBER','PARTITION BY','subquery'],altAnswers:[]},
{id:'mv42',db:'movies',difficulty:'advanced',title:'Actors in Multiple Movies',roleplay:'Find recurring actors.',task:'Find actors who appear in more than 1 movie using HAVING.',answer:'SELECT a.name, COUNT(DISTINCT c.movie_id) AS films FROM actors a JOIN cast_members c ON a.id = c.actor_id GROUP BY a.name HAVING COUNT(DISTINCT c.movie_id) > 1;',concepts:['HAVING','COUNT DISTINCT'],altAnswers:[]},
{id:'mv43',db:'movies',difficulty:'advanced',title:'NOT EXISTS Unreviewd',roleplay:'QA team finds gaps.',task:'Find movies that have no reviews using NOT EXISTS.',answer:'SELECT title FROM movies m WHERE NOT EXISTS (SELECT 1 FROM reviews r WHERE r.movie_id = m.id);',concepts:['NOT EXISTS'],altAnswers:[]},
{id:'mv44',db:'movies',difficulty:'advanced',title:'LAG Revenue Change',roleplay:'Year-over-year revenue comparison for Nolan.',task:'Use LAG to show previous movies revenue for each director.',answer:'SELECT title, director_id, release_year, revenue, LAG(revenue) OVER (PARTITION BY director_id ORDER BY release_year) AS prev_revenue FROM movies;',concepts:['LAG','PARTITION BY'],altAnswers:[]},
{id:'mv45',db:'movies',difficulty:'advanced',title:'Pct of Genre Revenue',roleplay:'How much does each movie contribute to its genre?',task:'Show each movies percentage of its genre total revenue.',answer:'SELECT title, genre, revenue, ROUND(revenue * 100.0 / SUM(revenue) OVER (PARTITION BY genre), 2) AS pct_of_genre FROM movies;',concepts:['SUM OVER','PARTITION BY'],altAnswers:[]},
{id:'mv46',db:'movies',difficulty:'advanced',title:'LEAD Next Movie',roleplay:'Show upcoming movie for each director.',task:'Use LEAD to show the next movie by same director chronologically.',answer:'SELECT title, director_id, release_year, LEAD(title) OVER (PARTITION BY director_id ORDER BY release_year) AS next_movie FROM movies;',concepts:['LEAD','PARTITION BY'],altAnswers:[]},
{id:'mv47',db:'movies',difficulty:'advanced',title:'Self Join Collaborations',roleplay:'Find actors who worked together.',task:'Find pairs of actors who appeared in the same movie.',answer:'SELECT a1.name AS actor1, a2.name AS actor2, c1.movie_id FROM cast_members c1 JOIN cast_members c2 ON c1.movie_id = c2.movie_id AND c1.actor_id < c2.actor_id JOIN actors a1 ON c1.actor_id = a1.id JOIN actors a2 ON c2.actor_id = a2.id;',concepts:['self-join'],altAnswers:[]},
{id:'mv48',db:'movies',difficulty:'advanced',title:'NTILE Budget Groups',roleplay:'Categorize movies into budget quartiles.',task:'Use NTILE(4) to divide movies into budget quartiles.',answer:'SELECT title, budget, NTILE(4) OVER (ORDER BY budget DESC) AS budget_quartile FROM movies;',concepts:['NTILE'],altAnswers:[]},
{id:'mv49',db:'movies',difficulty:'expert',title:'CTE Director Analysis',roleplay:'Deep director performance report.',task:'Use CTE to calculate each directors avg rating, total revenue, and movie count.',answer:'WITH dir_stats AS (SELECT director_id, COUNT(*) AS films, AVG(rating) AS avg_rating, SUM(revenue) AS total_rev FROM movies GROUP BY director_id) SELECT d.name, ds.films, ROUND(ds.avg_rating, 2) AS avg_rating, ds.total_rev FROM dir_stats ds JOIN directors d ON ds.director_id = d.id ORDER BY ds.avg_rating DESC;',concepts:['CTE'],altAnswers:[]},
{id:'mv50',db:'movies',difficulty:'expert',title:'Multi-CTE Report',roleplay:'Executive dashboard needs comprehensive data.',task:'Use multiple CTEs: one for movie stats per genre, one for top actors, combine them.',answer:"WITH genre_stats AS (SELECT genre, COUNT(*) AS count, AVG(rating) AS avg_rating, SUM(revenue) AS total_rev FROM movies GROUP BY genre), top_actors AS (SELECT a.name, COUNT(c.movie_id) AS films FROM actors a JOIN cast_members c ON a.id = c.actor_id GROUP BY a.name ORDER BY films DESC LIMIT 5) SELECT * FROM genre_stats UNION ALL SELECT 'TOP ACTORS', NULL, NULL, NULL WHERE 0;",concepts:['multiple CTEs'],altAnswers:["WITH genre_stats AS (SELECT genre, COUNT(*) AS count, ROUND(AVG(rating),2) AS avg_rating, SUM(revenue) AS total_rev FROM movies GROUP BY genre) SELECT * FROM genre_stats ORDER BY total_rev DESC;"]},
{id:'mv51',db:'movies',difficulty:'expert',title:'Recursive Billing',roleplay:'Show billing hierarchy.',task:'Create a CTE that numbers all cast appearances sequentially per movie.',answer:'WITH ordered_cast AS (SELECT m.title, a.name, c.billing_order, ROW_NUMBER() OVER (PARTITION BY c.movie_id ORDER BY c.billing_order) AS seq FROM cast_members c JOIN movies m ON c.movie_id = m.id JOIN actors a ON c.actor_id = a.id) SELECT * FROM ordered_cast ORDER BY title, seq;',concepts:['CTE','ROW_NUMBER'],altAnswers:[]},
{id:'mv52',db:'movies',difficulty:'expert',title:'Profitability Tiers',roleplay:'Investment analysis with complex tiers.',task:"Use CTE with CASE to create profitability tiers: 'Blockbuster' (>500M profit), 'Hit' (>100M), 'Moderate' (>0), 'Flop'.",answer:"WITH profits AS (SELECT title, revenue - budget AS profit FROM movies) SELECT title, profit, CASE WHEN profit > 500000000 THEN 'Blockbuster' WHEN profit > 100000000 THEN 'Hit' WHEN profit > 0 THEN 'Moderate' ELSE 'Flop' END AS tier FROM profits ORDER BY profit DESC;",concepts:['CTE','CASE WHEN'],altAnswers:[]},
{id:'mv53',db:'movies',difficulty:'expert',title:'Director Consistency',roleplay:'Find most consistent directors (lowest rating variance).',task:'Use CTE to find directors with the smallest difference between their best and worst rated movie.',answer:"WITH dir_range AS (SELECT director_id, MAX(rating) - MIN(rating) AS rating_range, COUNT(*) AS films FROM movies GROUP BY director_id HAVING COUNT(*) >= 2) SELECT d.name, dr.rating_range, dr.films FROM dir_range dr JOIN directors d ON dr.director_id = d.id ORDER BY dr.rating_range ASC;",concepts:['CTE','HAVING','aggregation'],altAnswers:[]},
{id:'mv54',db:'movies',difficulty:'expert',title:'Complete Film Report',roleplay:'Annual studio report generation.',task:'Create a comprehensive report: movie title, director, lead actor, rating, revenue rank, and review count.',answer:"WITH rev_ranked AS (SELECT id, title, director_id, rating, revenue, RANK() OVER (ORDER BY revenue DESC) AS rev_rank FROM movies), leads AS (SELECT c.movie_id, a.name AS lead_actor FROM cast_members c JOIN actors a ON c.actor_id = a.id WHERE c.billing_order = 1), rev_counts AS (SELECT movie_id, COUNT(*) AS review_count FROM reviews GROUP BY movie_id) SELECT r.title, d.name AS director, l.lead_actor, r.rating, r.rev_rank, COALESCE(rc.review_count, 0) AS reviews FROM rev_ranked r JOIN directors d ON r.director_id = d.id LEFT JOIN leads l ON r.id = l.movie_id LEFT JOIN rev_counts rc ON r.id = rc.movie_id ORDER BY r.rev_rank;",concepts:['multiple CTEs','COALESCE','LEFT JOIN','RANK'],altAnswers:[]},
];
